import { createTheme } from "@mui/material";

const ThemeConfigration = () => {
  const appytheme = () => {
    const theme = createTheme();
  };

  return <></>;
};

export default ThemeConfigration;
